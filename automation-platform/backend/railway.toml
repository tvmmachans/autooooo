[build]
builder = "nixpacks"
buildCommand = ""

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# Ensure Railway uses the backend directory
[build.env]
NODE_ENV = "production"

